# DEPRECATED — 수정 금지

이 폴더의 코드는 더 이상 쓰이지 않는다. 어디서도 import 하지 않는다.

**활성 결제 코드는 `src/payments/` 다.** 고칠 것도, 참고할 것도 거기에 있다.

여기 있는 `refund()`는 `src/payments/refund.js`와 이름만 같은 옛날 구현이다
(원 단위 정수, 에러 우선 콜백, `console.log`). 흉내내지 말 것.
