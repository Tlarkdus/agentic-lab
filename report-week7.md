# 조선소 보고서 — 미니 SaaS 진수

**제품**: `curve-doctor` — 학습 로그를 붙여넣으면 학습곡선의 문제 패턴과 다음 조치 한 개를 낸다
**저장소**: https://github.com/Tlarkdus/curve-doctor (public, `main`)
**검증**: `npm test` **53/53 pass** · PRD 합격선 샘플 **6/6** · phase 0 execute **재시도 0회**
**기간**: 2026-09-16 착공 ~ 2026-09-17 진수

> **7주차 강의 슬라이드는 끝내 확보하지 못했다.** 그래서 LAB 번호 대신 공정 단계로 적는다.
> SDD 하네스 명세는 `6주차_학습자료_자산공장_치트시트.pdf`(233~253행)에서,
> 하네스 실물 형식은 공개 예시 저장소 `jha0313/ch5-silicon-valley-vibe-coding-technique`의
> `scripts/execute.py` · `phases/0-mvp/index.json` · `step*.md`에서 가져왔다.

---

## 공정 점검표

| 단계 | 상태 | 산출물 | 작동 증거 |
|---|---|---|---|
| 1. DESIGN (명세) | ✅ | `docs/PRD.md`, `CLAUDE.md`, `docs/design.md` | 커밋 `96b9634` · Readiness 5체크 전항 [x] |
| 2. 감리(grill) | ✅ | PRD 보강분 | 구멍 **9건** 전부 반영, 설계 변경 **0건** |
| 3. PLAN (step 분해) | ✅ | `phases/0-mvp/step0~4.md` | 커밋 `98e16e6` — 슬라이스 5건 |
| 4. 수동 슬라이스 | ✅ | `src/thresholds.js`, `src/parse.js` | `e9c7fde`·`39efad2` — TDD RED→GREEN, 24/24 |
| 5. 하네스 이식 | ✅ | `scripts/execute.py` | `92fc817` · cp949 수정 `196d8a5` |
| 6. phase 0 자동 실행 | ✅ | step2·3·4 산출물 + `*-output.json` | `phases/index.json` `completed`, 재시도 0 |
| 7. 검문·병합·공개 | ✅ | 병합 커밋 `f6c4a3d`, `README.md` `339fbfb` | CRITICAL 5조 전항 통과, Risk **Low** |

---

## 1~2. DESIGN과 감리 — 코드보다 먼저 쓴 것

**산출물**: `docs/PRD.md`(한 줄 / What·입출력 타입 / Why·숫자 / Constraints / Boundaries / Edge 9항목 /
감리 기록 / Readiness 5체크) · `CLAUDE.md`(CRITICAL 5조) · `docs/design.md`(데이터 흐름·모듈 계약)

합격선을 문장이 아니라 **실행 커맨드**로 박았다.

> 준비한 로그 샘플 6종에서 의도한 code를 **6/6 정확히** 집어내는 것이 phase 0의 합격선이다.

**작동 증거** — 1차 감리에서 구멍 **9건**이 나왔고 전부 PRD에 반영했다. 보강 내역:
진단 조건의 수치 기준 명명(`DIVERGE_FACTOR` 등), 상호배제·순서 규칙, `ACTIONS` 고정 문자열표,
run 분리 기준, CSV epoch 컬럼 후보, `truncated` 필드 신설, `metrics`에 `null`을 넣지 않기로 확정.

**관찰** — **구멍 9건이 전부 빈칸이었지 오답이 아니었다.** 설계 변경은 한 건도 없었다.
되물어서 나온 건 "이 판정을 어떤 숫자로 하느냐", "무엇을 돌려주느냐"뿐이었다.
명세를 쓸 때 사람이 빠뜨리는 건 구조가 아니라 **경계값과 반환 형태**다. 그리고 그 빈칸들이
그대로 `src/thresholds.js` 한 파일이 됐다 — 감리 기록이 곧 상수 파일의 목차였다.

## 3~4. 수동 슬라이스 — 하네스에 넘기기 전에 사람이 두 칸

**산출물**: `src/thresholds.js`(`FINDING_CODES` 6 · `T` 임계 10종 · `SEVERITY` · `ACTIONS` ·
`FINDING_ORDER`, import 0·함수 0의 순수 데이터) · `src/parse.js`(`parseLog` / `lastRun`)

**작동 증거** — step0 4케이스, step1 21케이스. RED를 눈으로 확인하고 구현해 **24/24 pass**.
step1에서 실패한 1건은 구현이 아니라 **테스트 기댓값이 틀린 것**(중복 epoch은 같은 run)이라 기댓값을 정정했다.

**관찰** — **자동화에 넘길 것을 먼저 손으로 해봤다.** step0·1을 직접 돌려보지 않았다면 step 문서에
"RED를 확인하고 구현하라"를 쓸 수 없었다. 하네스는 사람이 이미 아는 절차를 복제하는 장치지,
모르는 절차를 발명해주는 장치가 아니다.

## 5~6. 하네스와 phase 0 자동 실행

**산출물**: `scripts/execute.py` — step 문서를 프롬프트로 만들어 `claude -p`에 넘기고,
브랜치 체크아웃·산출물 커밋·`index.json` 상태 갱신·재시도(최대 3)를 관리한다.

```
py scripts/execute.py phases/0-mvp     # python은 이 PC에서 스토어 스텁이라 py로 돈다
```

**작동 증거** — step2·3·4를 **사람 개입 없이** 완주했다. 각 step의 `summary`는 서브세션이 직접 적었다.

| step | 산출물 | 테스트 | 커밋 |
|---|---|---|---|
| 2. diagnose | `src/diagnose.js` — 6종 판정, 숫자 리터럴 0 | 43/43 | `540f559` |
| 3. samples | `tests/fixtures/*.log` 6종 + `samples.test.js` | 49/49 · **합격선 6/6** | `9bf1e74` |
| 4. format-ui | `src/format.js` + `index.html` | **53/53** | `95ff026` |

`phases/index.json` → `"status": "completed"`, 재시도 **0회**.

**관찰 ① — 죽은 건 일이 아니라 장부였다.**
하네스 1차 실행이 Windows **cp949**에서 죽었다. claude의 UTF-8 stdout을 `text=True`로 읽고
`✓`를 출력하다가 `UnicodeEncodeError`. 그런데 복구하고 보니 step2의 **코드·테스트·커밋은 멀쩡했다**.
날아간 건 하네스의 진행 출력과 chore 커밋뿐이었다. step 문서가 서브세션에게 *직접 커밋하라*고
시켜둔 덕이다. **오케스트레이터의 죽음이 작업의 죽음이 아니게 하려면, 작업이 자기 결과를 스스로
기록해야 한다.** 고친 커밋 `196d8a5`.

**관찰 ② — 서브에이전트가 고치지 않고 보고했다.**
step3에서 파서 한계를 발견했다. Keras 실제 2줄 출력의 진행바 줄
(`469/469 [====] - 3s 5ms/step - loss: 0.69`)은 이름 패턴이 공백·하이픈을 삼켜 `loss`가
`step_loss`로 잡힌다. 서브세션은 이걸 **고치지 않았다** — PRD에 문서화된 1줄 형태는 정상이고
step3의 범위는 샘플이지 파서가 아니었기 때문이다. `summary`에 "사람 판단 대기"로 적고 넘어갔다.
Surgical Changes가 명세에 박혀 있으면 **자율 실행이 범위를 넓히지 않는다**는 걸 실물로 봤다.

## 7. 검문·병합·공개

**작동 증거** — CRITICAL 5조 대조:

| 조 | 판정 | 근거 |
|---|---|---|
| ① 런타임 의존성 0 | ✅ | `package.json`에 `dependencies` 키 자체가 없음 |
| ② `src/`에 node 전용 API 금지 | ✅ | `require(`·`node:`·`fs`·`path` 0건 (`node:fs`는 테스트에만) |
| ③ 임계값은 `thresholds.js`만 | ✅ | `diagnose.js`는 `T.*`만 참조, 판정 코드에 숫자 리터럴 없음 |
| ④ 테스트 먼저 | ✅ | step별 RED 확인 기록, `src/*.js` 전부 대응 테스트 보유 |
| ⑤ `npm test` 통과 | ✅ | **53/53**, 병합 후 재확인 |

금지사항도 통과 — `fetch`·`XMLHttpRequest`·`localStorage`·외부 CDN **0건**.
Risk 5축 판정 **Low**(신규 파일 위주 +619/−16, 민감 경로 없음, 테스트 커버, 롤백 용이) → 그대로 병합.

`--no-ff`로 병합해 phase 경계를 이력에 남겼다(`f6c4a3d`). `feat-0-mvp` 브랜치는 증거로 원격에 함께 올렸다.

---

## 회고

### Agentic 축 — 에이전트에게 무엇을 주면 일이 되는가

**명세가 곧 권한 범위였다.** 자율 실행에서 무서운 건 에이전트가 못 하는 게 아니라 **너무 많이 하는
것**인데, phase 0에서 범위를 넘은 시도는 0건이었다. 막은 건 감시가 아니라 문서다 — `CLAUDE.md`의
CRITICAL 5조, step 문서의 "이 파일만 건드린다", PRD의 Boundaries 6항목. step3이 파서 버그를 보고
**고치지 않고 적어둔** 것이 그 증거다. 에이전트에게 줄 것은 할 일 목록이 아니라 **하지 않을 일의 목록**이다.

그리고 **되물음이 코드보다 쌌다.** 감리 9건은 전부 빈칸이었고, 그 빈칸을 코딩 전에 메운 덕에
step2~4가 재시도 0회로 통과했다. 명세에서 30분 더 쓴 값이 재시도 0으로 돌아왔다.

### Harness 축 — 자동화를 자동화하면 생기는 일

**하네스는 일을 하지 않는다. 일의 *장부*를 쓴다.** cp949 사고가 이걸 갈라서 보여줬다 —
오케스트레이터가 죽었는데 코드는 살아 있었다. 그래서 하네스 설계의 핵심은 실행이 아니라
**체크포인트를 누가 쓰느냐**였다. 서브세션이 자기 손으로 커밋하면 오케스트레이터는 언제 죽어도 된다.

**가드는 세션의 프로젝트 루트를 본다.** agentic-lab에서 세션을 연 채 curve-doctor 파일을 만지자
agentic-lab의 TDD 가드가 그걸 막았다. 가드가 `CLAUDE_PROJECT_DIR`(=agentic-lab) 기준으로 `tests/`를
찾기 때문에, **파일이 속한 저장소가 아니라 세션이 열린 저장소**를 기준으로 판정한 것이다.
훅은 파일의 소속을 모른다 — 저장소를 옮겨 일할 땐 세션도 같이 옮겨야 한다.

**실패는 이번에도 조용했다.** `python`은 스토어 스텁이라 아무 말 없이 아무것도 하지 않았고,
cp949는 진행 출력만 지우고 커밋은 남겨 "성공인지 실패인지 모를 상태"를 만들었다.
8주 내내 같은 교훈이 반복된다 — **밖에 나가서, 없어야 할 것이 없는지 직접 확인한다.**

### 다음 항해

phase 0은 **MVP지 제품이 아니다.** 남은 것:

1. **파서 한계 결정** — Keras 2줄 진행바 형식을 지원할지, PRD의 1줄 형태로 범위를 고정할지.
   step3이 사람 판단으로 넘긴 유일한 항목이다.
2. **phase 1** — 같은 하네스로 다음 슬라이스를 돌린다. 이번엔 step0부터 자동으로.
3. **브라우저 실물 확인** — `index.html`은 비대화형 세션이라 열지 못했고,
   같은 모듈 체인을 node로 통과시키는 것으로 갈음했다. 눈으로 한 번 봐야 한다.

배는 떴다. 실을 짐은 다음 phase다.
